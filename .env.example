# Hosted API base URL, e.g. https://api.example.com
# Consumed by the Vite dev proxy in vite.config.ts (restart dev server after editing).
VITE_API_TARGET=

# Access key sent on each request by src/api/client.ts
VITE_API_KEY=
